ops:
    - description: Prove bash routing
      op: write_fixture
      args: output/bash-routing-ok.json, fixtures/dispatch/ok.json
      content: |
        {"status":"ok","source":"harness-dispatch-behaviour-test"}
    - description: Emit review JSON
      op: write_fixture
      args: output/agent-result.json, fixtures/review/comment.json
      content: |
        {
          "action": "comment",
          "pr_number": 1,
          "repo": "owner/repo",
          "head_sha": "abcdef0123456789abcdef0123456789abcdef01",
          "body": "Behaviour smoke: bash-routed review completed."
        }
